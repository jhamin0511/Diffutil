package com.github.jhamin0511.diffutil.data.mapper

import com.github.jhamin0511.diffutil.data.dto.User
import com.github.jhamin0511.diffutil.data.vo.ListVo
import java.text.SimpleDateFormat
import java.util.*

object UserMapper {

//    fun toList(value: List<User>): List<ListVo> {
//        private val format = SimpleDateFormat("yyyy.MM.dd", Locale.getDefault())
//
//        val sorted = value.sortedBy { it.createdAt }
//        val group = sorted.groupBy { format.format(it.createdAt) }
//
//        group.forEach(
//
//        )
//
//        return ListVo(value.id, value.name, value.age)
//    }

}
